# Captive portal: while wlan0 is the shared-mode AP, resolve ALL names to the Pi
# (10.42.0.1) so every client HTTP request hits the captive-portal responder on :80.
# Only affects the AP's shared dnsmasq instance; home-wifi DNS is unaffected.
address=/#/10.42.0.1
